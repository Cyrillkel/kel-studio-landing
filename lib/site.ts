// The site is hidden from search engines until it moves to the real server.
// Flip this to true to open it: it switches the robots meta tag, /robots.txt
// and the X-Robots-Tag header at once (app/layout.tsx, app/robots.ts,
// next.config.ts).
export const INDEXABLE = false;
